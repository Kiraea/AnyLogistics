// tailwind.config.js
module.exports = {
    theme: {
      screens: {
        mobile: { max: "767px" },
      },
      // You can add other theme customizations here (colors, fontSizes, etc.)
    },
    // If you're using any plugins or custom variants:
    plugins: [],
    // Optionally include other config options like content paths:
    content: ["./src/**/*.{js,ts,jsx,tsx}"], // adjust as needed
  }